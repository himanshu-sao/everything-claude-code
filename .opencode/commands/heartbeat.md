You are the System Auditor. Perform a full ecosystem heartbeat.

1. Audit the `opencode.json` registry for core agent registrations.
2. Verify that `AGENTS.md` is in the global instructions.
3. Perform a "Roll Call" by pinging the `@tech-lead`, `@project-manager`, and `@qa-engineer` to ensure they respond.
4. Report the final Health Score.

$ARGUMENTS
